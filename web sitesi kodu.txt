<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hamdi Gökmen Solak | Digital Portfolio</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700&family=Rajdhani:wght@500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* --- 1. CORE VISUALS --- */
        :root {
            --bg-dark: #050505;
            --glass-border: rgba(255, 255, 255, 0.1);
            --glass-bg: rgba(255, 255, 255, 0.03);
            --neon-blue: #00f2ff;
            --neon-purple: #bd00ff;
            --text-main: #ffffff;
            --text-muted: #94a3b8;
        }

        body {
            margin: 0; padding: 0;
            background-color: var(--bg-dark);
            font-family: 'Outfit', sans-serif;
            color: var(--text-main);
            overflow-x: hidden;
        }

        /* Mouse Takip Eden Işık */
        .spotlight {
            position: fixed; top: 0; left: 0;
            width: 100vw; height: 100vh;
            pointer-events: none; z-index: 1;
            background: radial-gradient(600px circle at var(--x) var(--y), rgba(29, 78, 216, 0.15), transparent 40%);
        }

        /* Arka Plan Canvas */
        #network-canvas {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            z-index: 0; opacity: 0.6;
        }

        .container {
            position: relative; z-index: 2;
            max-width: 1100px; margin: 0 auto; padding: 20px;
        }

        /* --- 2. HERO SECTION --- */
        .hero {
            min-height: 95vh;
            display: flex; flex-direction: column;
            justify-content: center; align-items: center;
            text-align: center;
            padding-top: 40px;
        }

        .profile-container {
            position: relative; width: 180px; height: 180px; margin-bottom: 30px;
        }

        .profile-glow {
            position: absolute; inset: -10px;
            background: conic-gradient(from 0deg, transparent, var(--neon-blue), transparent, var(--neon-purple), transparent);
            border-radius: 50%;
            animation: spin 4s linear infinite;
            filter: blur(20px); opacity: 0.7;
        }
        @keyframes spin { 100% { transform: rotate(360deg); } }

        .profile-img {
            position: relative; width: 100%; height: 100%;
            border-radius: 50%; object-fit: cover;
            border: 2px solid rgba(255,255,255,0.2);
            z-index: 2; background: #000;
        }

        h1 {
            font-family: 'Rajdhani', sans-serif; font-size: 3.5rem; margin: 0;
            background: linear-gradient(to right, #fff, #94a3b8);
            -webkit-background-clip: text; color: transparent;
            letter-spacing: 2px; animation: fadeUp 1s ease forwards; opacity: 0;
        }

        /* --- BUTONLAŞTIRILMIŞ ROZETLER --- */
        .subtitle {
            margin-top: 25px; display: flex; gap: 15px; justify-content: center; flex-wrap: wrap;
            animation: fadeUp 1s ease 0.3s forwards; opacity: 0;
        }
        
        .badge {
            padding: 10px 25px; border-radius: 30px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.2);
            font-size: 0.9rem; font-weight: 500;
            display: flex; align-items: center; gap: 8px;
            cursor: pointer; transition: 0.3s all ease;
            user-select: none;
        }

        .badge:hover {
            transform: translateY(-3px);
            border-color: var(--neon-blue);
            box-shadow: 0 0 15px rgba(0, 242, 255, 0.2);
        }

        /* Aktif Olduğunda (Tıklanınca) */
        .badge.active {
            background: var(--neon-blue);
            color: #000;
            border-color: var(--neon-blue);
            box-shadow: 0 0 25px rgba(0, 242, 255, 0.6);
            font-weight: 700;
        }
        /* Campus için özel renk (Mor) */
        .badge.purple-mode.active {
            background: var(--neon-purple);
            border-color: var(--neon-purple);
            box-shadow: 0 0 25px rgba(189, 0, 255, 0.6);
            color: #fff;
        }

        /* --- DİNAMİK BIO ALANI --- */
        .bio-text {
            max-width: 800px; min-height: 200px; /* Sabit yükseklik zıplamayı önler */
            color: #d1d5db; margin-top: 40px; font-size: 1.1rem; line-height: 1.8;
            animation: fadeUp 1s ease 0.6s forwards; opacity: 0;
            background: linear-gradient(180deg, rgba(255,255,255,0.05) 0%, transparent 100%);
            padding: 30px; border-radius: 15px;
            border: 1px solid rgba(255,255,255,0.1);
            backdrop-filter: blur(5px);
            transition: 0.3s;
        }
        
        /* Metin değişirken efekt */
        .fade-in-text { animation: textFade 0.5s ease forwards; }
        @keyframes textFade { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        .bio-text strong { color: var(--neon-blue); font-weight: 500; }

        /* --- DİĞER BÖLÜMLER (GRID & CARDS) --- */
        .section-title {
            font-family: 'Rajdhani', sans-serif; font-size: 2.5rem; margin: 100px 0 50px;
            text-align: center; color: var(--text-main); text-transform: uppercase; letter-spacing: 3px;
        }
        .section-title span { color: var(--neon-purple); }

        .grid-container { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 25px; }

        .glass-card {
            background: var(--glass-bg); border: 1px solid var(--glass-border);
            border-radius: 20px; padding: 30px; backdrop-filter: blur(10px);
            transition: transform 0.1s; position: relative; overflow: hidden;
            transform-style: preserve-3d; perspective: 1000px;
            display: flex; flex-direction: column;
        }
        .glass-card::before {
            content: ""; position: absolute; top: 0; left: 0; right: 0; bottom: 0;
            border-radius: 20px; padding: 2px;
            background: linear-gradient(135deg, rgba(255,255,255,0.1), transparent);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor; mask-composite: exclude; pointer-events: none;
        }
        .glass-card:hover { border-color: rgba(255, 255, 255, 0.3); box-shadow: 0 0 30px rgba(0, 242, 255, 0.1); }

        .card-icon { font-size: 2.5rem; color: var(--neon-blue); margin-bottom: 20px; transform: translateZ(30px); }
        .card-title { font-size: 1.5rem; font-weight: 700; margin-bottom: 10px; transform: translateZ(20px); }
        .card-desc { color: var(--text-muted); line-height: 1.6; transform: translateZ(10px); font-size: 0.95rem; }

        /* Tech Tags */
        .tech-category { margin-bottom: 25px; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 20px; }
        .tech-category:last-child { border-bottom: none; }
        .cat-title { font-size: 1.1rem; font-weight: 600; margin-bottom: 10px; display: block; }
        .blue-text { color: var(--neon-blue); } .purple-text { color: var(--neon-purple); }
        .cat-desc { font-size: 0.9rem; color: #aaa; margin-bottom: 15px; display: block; }
        .tag-container { display: flex; flex-wrap: wrap; gap: 10px; }
        .tech-tag { padding: 6px 14px; border-radius: 8px; font-size: 0.85rem; font-weight: 500; transition: 0.3s; cursor: default; }
        .tag-blue { background: rgba(0, 242, 255, 0.05); border: 1px solid rgba(0, 242, 255, 0.2); color: var(--neon-blue); }
        .tag-blue:hover { background: rgba(0, 242, 255, 0.2); box-shadow: 0 0 10px rgba(0, 242, 255, 0.2); }
        .tag-purple { background: rgba(189, 0, 255, 0.05); border: 1px solid rgba(189, 0, 255, 0.2); color: var(--neon-purple); }
        .tag-purple:hover { background: rgba(189, 0, 255, 0.2); box-shadow: 0 0 10px rgba(189, 0, 255, 0.2); }

        /* Footer */
        .cta-section { text-align: center; margin: 100px 0; padding: 60px; background: linear-gradient(180deg, transparent, rgba(0, 242, 255, 0.05)); border-radius: 20px; }
        .neon-btn { display: inline-block; margin-top: 30px; padding: 15px 50px; background: var(--text-main); color: #000; font-weight: 700; text-transform: uppercase; border-radius: 30px; box-shadow: 0 0 20px rgba(255, 255, 255, 0.3); transition: 0.3s; text-decoration: none; letter-spacing: 1px; }
        .neon-btn:hover { background: var(--neon-blue); box-shadow: 0 0 40px var(--neon-blue); transform: scale(1.05); }
        .social-link { color: #fff; margin: 0 15px; transition: 0.3s; font-size: 1.8rem; display: inline-block; }
        .social-link:hover { color: var(--neon-blue); transform: scale(1.2); text-shadow: 0 0 15px var(--neon-blue); }

        @keyframes fadeUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
        @media (max-width: 768px) { h1 { font-size: 2.2rem; } .subtitle { flex-direction: column; gap: 10px; } .bio-text { padding: 20px; font-size: 1rem; } }
    </style>
</head>
<body>

    <div class="spotlight"></div>
    <canvas id="network-canvas"></canvas>

    <div class="container">
        
        <section class="hero">
            <div class="profile-container">
                <div class="profile-glow"></div>
                <img src="profil.jpg" alt="Hamdi Gökmen Solak" class="profile-img" onerror="this.src='https://via.placeholder.com/200'">
            </div>

            <h1>HAMDI GÖKMEN SOLAK</h1>
            
            <div class="subtitle">
                <div class="badge" onclick="updateBio('cyber', this)">
                    <i class="fas fa-shield-alt"></i> Cyber Security
                </div>
                <div class="badge" onclick="updateBio('ai', this)">
                    <i class="fas fa-brain"></i> AI & Data Science
                </div>
                <div class="badge purple-mode" onclick="updateBio('campus', this)">
                    <i class="fas fa-users"></i> Campus Representative
                </div>
            </div>

            <div class="bio-text" id="bio-content">
                <p>
                    Karadeniz Teknik Üniversitesi Yönetim Bilişim Sistemleri bölümünde, teknolojinin iş dünyası ve güvenlik süreçleriyle kesiştiği noktada uzmanlaşıyorum.
                </p>
                <p style="margin-top: 15px;">
                    Temel odağım, <strong>'AI for Security' (Güvenlik için Yapay Zeka)</strong> vizyonudur. Geleneksel siber güvenlik yöntemlerinin ötesine geçerek, yapay zeka algoritmalarıyla güçlendirilmiş, proaktif ve otonom savunma sistemleri geliştirmeyi hedefliyorum.
                </p>
                <p style="margin-top: 15px;">
                    Aşağıdaki başlıklara tıklayarak ilgi alanlarımın detaylarını görebilirsiniz.
                </p>
            </div>
        </section>

        <h2 class="section-title">Deneyim & <span>Yolculuk</span></h2>
        <div class="grid-container">
            <div class="glass-card tilt-card">
                <div class="card-icon"><i class="fas fa-briefcase"></i></div>
                <div class="card-title">SCA Social</div>
                <p class="card-desc"><strong>Proje Yöneticisi (Staj)</strong><br>Proje süreçlerinin uçtan uca yönetimi, dokümantasyon, performans takibi ve zaman çizelgelerinin optimize edilmesi.</p>
            </div>
            <div class="glass-card tilt-card">
                <div class="card-icon"><i class="fas fa-users"></i></div>
                <div class="card-title">Etkin Kampüs</div>
                <p class="card-desc"><strong>Kampüs Temsilcisi</strong><br>Üniversite öğrencileri ile sektör profesyonellerini buluşturan etkinliklerin organizasyonu, aktif networking ve topluluk yönetimi.</p>
            </div>
            <div class="glass-card tilt-card">
                <div class="card-icon"><i class="fas fa-user-graduate"></i></div>
                <div class="card-title">Akademik Eğitim</div>
                <p class="card-desc"><strong>KTÜ - YBS</strong><br>Algoritma, Veri Tabanı Yönetimi ve İşletme Stratejileri üzerine lisans eğitimi (2024 - Günümüz).</p>
            </div>
        </div>

        <h2 class="section-title">Profesyonel <span>Yetkinlikler</span></h2>
        <div class="grid-container">
            <div class="glass-card tilt-card">
                <div class="card-icon"><i class="fas fa-code"></i></div>
                <h3 style="margin:0 0 20px 0; color:#fff;">Teknik Altyapı</h3>
                <div class="tech-category">
                    <span class="cat-title blue-text">Yazılım & Yapay Zeka</span>
                    <span class="cat-desc">Veri analizi, otomasyon ve model geliştirme araçları.</span>
                    <div class="tag-container">
                        <span class="tech-tag tag-blue">Python</span>
                        <span class="tech-tag tag-blue">Pandas / NumPy</span>
                        <span class="tech-tag tag-blue">Scikit-learn</span>
                        <span class="tech-tag tag-blue">Otomasyon</span>
                    </div>
                </div>
                <div class="tech-category">
                    <span class="cat-title blue-text">Siber Güvenlik & Sistem</span>
                    <span class="cat-desc">Sızma testi araçları, ağ analizi ve işletim sistemleri.</span>
                    <div class="tag-container">
                        <span class="tech-tag tag-blue">Kali Linux</span>
                        <span class="tech-tag tag-blue">Nmap & Wireshark</span>
                        <span class="tech-tag tag-blue">Burp Suite</span>
                        <span class="tech-tag tag-blue">TCP/IP & Network</span>
                        <span class="tech-tag tag-blue">Bash Scripting</span>
                    </div>
                </div>
            </div>
            
            <div class="glass-card tilt-card">
                <div class="card-icon"><i class="fas fa-handshake"></i></div>
                <h3 style="margin:0 0 20px 0; color:#fff;">Sosyal & Yönetim</h3>
                <div class="tech-category">
                    <span class="cat-title purple-text">Proje & Takım Yönetimi</span>
                    <span class="cat-desc">Ekip koordinasyonu, kriz yönetimi ve süreç planlama.</span>
                    <div class="tag-container">
                        <span class="tech-tag tag-purple">Proje Dokümantasyonu</span>
                        <span class="tech-tag tag-purple">Zaman Yönetimi</span>
                        <span class="tech-tag tag-purple">Kriz İletişimi</span>
                        <span class="tech-tag tag-purple">Liderlik</span>
                    </div>
                </div>
                <div class="tech-category">
                    <span class="cat-title purple-text">İletişim & Organizasyon</span>
                    <span class="cat-desc">Topluluk oluşturma ve profesyonel ağ yönetimi.</span>
                    <div class="tag-container">
                        <span class="tech-tag tag-purple">Networking</span>
                        <span class="tech-tag tag-purple">Etkinlik Organizasyonu</span>
                        <span class="tech-tag tag-purple">Topluluk Yönetimi</span>
                        <span class="tech-tag tag-purple">Sunum Becerisi</span>
                    </div>
                </div>
            </div>
        </div>

        <div class="cta-section">
            <h2 style="font-family:'Rajdhani'; font-size:3rem; margin:0;">BİRLİKTE ÇALIŞALIM</h2>
            <p style="color:#94a3b8; margin-top:10px;">Geleceğin projelerini inşa etmek için hazırım.</p>
            <a href="mailto:solakgokmen@gmail.com" class="neon-btn">İletişime Geç</a>
            
            <div style="margin-top: 40px;">
                <a href="https://www.linkedin.com/in/hamdi-gökmen-solak-170105280" target="_blank" class="social-link"><i class="fab fa-linkedin"></i></a>
                <a href="https://github.com/GokmenSolak" target="_blank" class="social-link"><i class="fab fa-github"></i></a>
                <a href="https://www.instagram.com/gokmen_solakk" target="_blank" class="social-link"><i class="fab fa-instagram"></i></a>
            </div>
        </div>

        <footer style="text-align: center; padding-bottom: 30px; color: #555; font-size: 0.9rem;">
            © 2024 Hamdi Gökmen Solak | Designed with Future Tech
        </footer>

    </div>

    <script>
        // --- 1. ETKİLEŞİMLİ BIO (YENİ) ---
        const bioData = {
            cyber: `
                <h3 class="blue-text" style="margin-top:0">Hedef: Proaktif Savunma</h3>
                <p>Siber güvenlikte sadece saldırıları beklemek yerine, <strong>ofansif (Red Team)</strong> yetkinliklerle açıkları önceden tespit etmeye odaklanıyorum.</p>
                <p style="margin-top:15px">Siber Vatan eğitimleri ve CTF deneyimlerimle, ağ güvenliği, sızma testleri ve zararlı yazılım analizi konularında teknik derinliğimi artırıyorum. Amacım, karmaşık siber tehditleri nötralize eden güvenli mimariler kurmaktır.</p>
            `,
            ai: `
                <h3 class="blue-text" style="margin-top:0">Hedef: AI Destekli Güvenlik</h3>
                <p>Yapay zeka, siber savunmanın geleceğidir. Python ve Veri Bilimi araçlarını (Pandas, Scikit-learn) kullanarak güvenlik loglarını analiz eden ve anomali tespiti yapan modeller geliştiriyorum.</p>
                <p style="margin-top:15px">Hedefim, <strong>AI for Security</strong> vizyonuyla, insan müdahalesine gerek kalmadan tehditleri saniyeler içinde algılayan otonom sistemler üretmektir.</p>
            `,
            campus: `
                <h3 class="purple-text" style="margin-top:0">Hedef: Güçlü Ağ & Liderlik</h3>
                <p>Teknik bilginin, doğru iletişimle değere dönüştüğüne inanıyorum. <strong>Etkin Kampüs Temsilcisi</strong> olarak, öğrenci toplulukları ile sektör profesyonelleri arasında köprü kuruyorum.</p>
                <p style="margin-top:15px">Etkinlik yönetimi, ekip koordinasyonu ve networking becerilerimle, hem kendimi hem de içinde bulunduğum ekosistemi geliştiriyorum.</p>
            `
        };

        const bioContainer = document.getElementById('bio-content');
        const badges = document.querySelectorAll('.badge');

        function updateBio(type, element) {
            // Aktif sınıfını diğerlerinden kaldır
            badges.forEach(b => b.classList.remove('active'));
            // Tıklanana ekle
            element.classList.add('active');

            // İçeriği değiştir ve animasyon ekle
            bioContainer.innerHTML = bioData[type];
            bioContainer.classList.remove('fade-in-text');
            void bioContainer.offsetWidth; // Trigger Reflow (Animasyonu resetlemek için)
            bioContainer.classList.add('fade-in-text');
        }

        // --- 2. GÖRSEL EFEKTLER (MEVCUT) ---
        const spotlight = document.querySelector('.spotlight');
        document.addEventListener('mousemove', (e) => {
            spotlight.style.setProperty('--x', e.clientX + 'px');
            spotlight.style.setProperty('--y', e.clientY + 'px');
        });

        const cards = document.querySelectorAll('.tilt-card');
        cards.forEach(card => {
            card.addEventListener('mousemove', (e) => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                const rotateX = ((y - centerY) / centerY) * -10;
                const rotateY = ((x - centerX) / centerX) * 10;
                card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
            });
            card.addEventListener('mouseleave', () => {
                card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) scale(1)';
            });
        });

        const canvas = document.getElementById('network-canvas');
        const ctx = canvas.getContext('2d');
        let width, height;
        function resize() { width = canvas.width = window.innerWidth; height = canvas.height = window.innerHeight; }
        window.addEventListener('resize', resize);
        resize();

        const particles = [];
        const particleCount = 70;
        class Particle {
            constructor() {
                this.x = Math.random() * width; this.y = Math.random() * height;
                this.vx = (Math.random() - 0.5) * 0.5; this.vy = (Math.random() - 0.5) * 0.5;
                this.size = Math.random() * 2 + 1;
            }
            update() {
                this.x += this.vx; this.y += this.vy;
                if (this.x < 0 || this.x > width) this.vx *= -1;
                if (this.y < 0 || this.y > height) this.vy *= -1;
            }
            draw() {
                ctx.beginPath(); ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(0, 242, 255, 0.4)'; ctx.fill();
            }
        }
        for(let i=0; i<particleCount; i++) particles.push(new Particle());
        function animate() {
            ctx.clearRect(0, 0, width, height);
            particles.forEach((p, index) => {
                p.update(); p.draw();
                for (let j = index; j < particles.length; j++) {
                    const p2 = particles[j];
                    const dx = p.x - p2.x; const dy = p.y - p2.y;
                    const dist = Math.sqrt(dx*dx + dy*dy);
                    if (dist < 150) {
                        ctx.beginPath();
                        ctx.strokeStyle = `rgba(0, 242, 255, ${0.1 - dist/1500})`;
                        ctx.lineWidth = 1; ctx.moveTo(p.x, p.y); ctx.lineTo(p2.x, p2.y); ctx.stroke();
                    }
                }
            });
            requestAnimationFrame(animate);
        }
        animate();
    </script>
</body>
</html>